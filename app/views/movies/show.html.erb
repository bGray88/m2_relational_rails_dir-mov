<br><br><%= link_to "Update #{@movie.name}", "/movies/#{@movie.id}/edit" %><br>
<%= link_to "Delete #{@movie.name}", "/movies/#{@movie.id}", method: :delete %><br><br>

<%= label :text, :name, "Name: #{@movie.name}" %><br>
<%= label :text, :version, "Version: #{@movie.version}" %><br>
<%= label :text, :rating, "Rating: #{@movie.rating}" %><br>
<%= label :text, :censored, "Censored: #{@movie.censored}" %><br>
<%= label :text, :length_in_mins, "Length #{@movie.length_in_mins} minutes" %>
